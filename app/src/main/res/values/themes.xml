<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Tema principal do app -->
    <style name="Theme.Cartbutler" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/primary_text_dark</item> <!-- Cor de fundo padrão -->
    </style>

    <!-- Tema da Splash Screen para versões anteriores ao Android 12 -->
    <style name="Theme.Cartbutler.Splash" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_background</item> <!-- Chama o logo -->
    </style>
</resources>